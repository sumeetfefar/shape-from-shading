clear all

load('DataFile1.m')
load('DataFile2.m')

M = size(R_est,1);
N = size(R_est,2);

iters = 1000;

zn = zeros(size(R_est));
zold = zeros(size(R_est));

for kk = 1:iters,
    disp(kk)
    for i=2:(M-1),
        for j=2:(N-1),
            zn(i,j) = (zold(i,j-1)+zold(i-1,j)+zold(i,j+1)+zold(i+1,j))/4 + pn(i,j)*i+q(
        end
    end
    p_old = pn;
    q_old = qn;
end